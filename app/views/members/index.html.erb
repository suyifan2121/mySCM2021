<body class="hold-transition skin-blue sidebar-mini">
  <div class="wrapper">
    <%= render 'layouts/header' %>
    <div class="content-wrapper">

      <!-- Content Header (Page header) -->
      <section class="content-header">
        <h1>
          Members
          <small>Management</small>
        </h1>
        <ol class="breadcrumb">
          <li>
            <a href="#">
              <i class="fa fa-home"></i>
              Home Page</a>
          </li>
          <li class="active">Manage Members</li>
        </ol>
      </section>

      <section class="content">
        <div class="row">
          <div class="col-xs-12">
            <!-- Main data table -->
            <div class="box">

              <!-- /.box-header -->
              <div class="box-body">
                <table id="example1" class="table table-bordered table-striped">
                  <thead>
                    <tr>
                      <th>ID</th>
                      <th>Name</th>
                      <th>Email</th>
                      <th>Phone</th>
                      <th>Role</th>
                      <th>Edit</th>
                      <!-- remark: hide the delete button from unauthorized users -->
                      <% if can? :delete, Member %>
                        <th>Delete</th>
                      <% end %>
                    </tr>
                  </thead>
                  <tbody>
                    <% @members.each do |member| %>
                    <tr>
                      <td><%= member.id %></td>
                      <td><%= member.name %></td>
                      <td><%= member.email %></td>
                      <td><%= member.phone %></td>
                      <td><%= member.get_role %></td>
                      <td><%= link_to 'Edit', edit_member_path(member) %></td>
                      <!-- remark: hide the delete button from unauthorized users -->
                      <% if can? :delete, Member %>
                        <td><%= link_to 'Delete', member, method: :delete, data: { confirm: 'Are you sure you want to delete this member?' } %></td>
                      <% end %>
                    </tr>
                    <% end %>
                  </tbody>
                </table>
              </div>
              <!-- /.box-body -->
            </div>
          </div>
        </div>
      </section>


    </div>
    <%= render 'layouts/footer' %>
  </div>
  <script>
    $(function () {
      $("#example1").DataTable();
      
    });
  </script>
</body>

